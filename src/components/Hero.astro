---
import { Image } from "astro:assets";

interface Props {
  bgImage: ImageMetadata;
  bgImageMobile: ImageMetadata;
}

const { bgImage, bgImageMobile } = Astro.props;
---

<section
  class="p-6 md:p-10 relative bg-cover bg-center w-full h-screen overflow-hidden"
>
  <picture>
    <source media="(max-width: 799px)" srcset={bgImageMobile.src} />
    <source media="(min-width: 800px)" srcset={bgImage.src} />
    <Image src={bgImage} alt="Background image" class="w-full h-full object-cover" />
  </picture>
  <slot />
</section>

<style>
  picture {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }
</style>